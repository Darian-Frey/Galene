//! Environment commands — list, select, and set richness for environments.

// TODO(phase-1): list_environments(), select_environment(id),
// set_richness(value), preview_environment(id) — backed by the scene loader in
// flowstate-visual and the patch library in flowstate-audio.
