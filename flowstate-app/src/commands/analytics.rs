//! Analytics commands — daily / weekly / environment views and insights.

// TODO(phase-2): daily_view(), weekly_view(), environment_insights(),
// focus_health() — reading from flowstate_core::analytics.
