//! Session commands — begin / pause / resume / end a focus session.

// TODO(phase-2): begin_session(env_id, session_type, intention),
// pause/resume, end_session(quality) — driving flowstate_core::FocusSession and
// the work↔break environmental transitions.
